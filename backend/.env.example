NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=zera
DB_PASSWORD=zera_dev_password
DB_NAME=zera_erp

JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=change_me_too_in_production
JWT_REFRESH_EXPIRES_IN=7d

# Bootstrap Super Admin, used only by `npm run seed` (registration is invite-only)
SEED_SUPER_ADMIN_EMAIL=noushad@zeracreative.com
SEED_SUPER_ADMIN_PASSWORD=change_me_immediately

# Comma-separated list of origins allowed to call this API (no wildcard).
# Falls back to the local Flutter-web dev server (http://localhost:5050)
# when unset.
CORS_ORIGIN=http://localhost:5050
