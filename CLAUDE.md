# Zera Creative ERP — Project Reference

## Role
Lead Software Architect / Senior Full-Stack Engineer building Zera Creative's internal ERP platform. Long-term production system, not a prototype. Every decision favors scalability, maintainability, security, performance, and clean code over convenience. When multiple approaches are viable, pick the one common in mature enterprise software.

## Vision
Primary internal operating system for Zera Creative, gradually replacing internal tools while staying modular for future expansion. Feels like Linear/Notion/Slack/ClickUp (modern SaaS), functions like Odoo (ERP-style). Visual design: modern SaaS, not traditional ERP chrome.

## Core Principles
Clean Architecture, modular design, SOLID, maintainability, scalability, security, performance, readability, reusability, testability, strong DX. No unnecessary technical debt.

## Tech Stack

### Frontend — Flutter (single shared codebase)
- Platforms: Web, Android, iOS, Windows, macOS, Linux
- Material Design 3
- Responsive nav: Desktop = sidebar, Tablet = collapsible sidebar, Mobile = bottom nav
- Fonts: Poppins for all headings/titles/section titles/table headers/dialog titles/nav headings (heading font only). Inter (or similar) for body text.

### Backend
- REST API, PostgreSQL, JWT auth, RBAC, Docker, env-based config
- Repository Pattern, Dependency Injection
- Framework/language: **not yet decided — confirm with user before scaffolding**

### Project Structure (feature-modules, avoid circular deps; shared code in `shared/core`)
```
features/
  authentication/
  employee/
  departments/
  knowledge_base/
  assets/
  leave/
  requests/
  feedback/
  notifications/
  settings/
shared/
core/
```
Each feature: presentation / application / domain / data layers.

## Branding
- Company: Zera Creative
- Colors (updated to a modern-SaaS palette, see `frontend/lib/core/theme/app_colors.dart`): background white `#FFFFFF`, canvas/fields softly lavender-tinted `#F5F4FB`/`#F1F0F9`, primary/brand violet `#6C5DD3` with a soft tint `#EDEBFC`, secondary blue `#4F8EF7` and the original Zera cyan `#00D5EE` kept as a tertiary accent — used together across stat tiles, charts, and progress indicators rather than a single flat accent. Near-black `#181425` for high-contrast tiles and the active nav pill. Text dark gray/near-black, cards white with soft shadows (not just borders), plus standard success/warning/error status colors (each with a soft tint for pill badges/tiles).
- Shape language: generous rounded corners (cards ~18px, buttons/inputs ~12px, pills fully rounded), soft drop shadows instead of flat borders on cards, colorful soft-tinted stat tiles (`MetricCard`) rather than plain white-with-colored-number tiles.
- Logo: official Zera Creative logo (not yet supplied in repo) goes in `assets/branding/`; used on login, sidebar, header, splash, app icon, favicon. Never recreate/modify it.
- Feel: modern SaaS dashboard — clean and spacious, but colorful/lively rather than monochrome-minimal.

## Authentication
- Company-email-only registration (`firstname.lastname@zeracreative.com`), no public registration
- Super Admin / HR invite employees
- Email verification, password reset/change, session management, login history, device history, optional 2FA

## RBAC
Flexible/configurable, not hardcoded. Default roles: Super Admin, HR/Manager, Team Lead, Employee. Must support custom roles without app-logic changes.

## Feature Modules (V1)
Role-specific dashboards (Employee/Team Lead/HR-Manager/Super Admin), Employee Management (full profile fields incl. profile completion %), Departments (no separate Teams concept — departments cover team groupings), Company Notices (targeting + read receipts + comments), Knowledge Base (Super Admin/HR-Manager/Team Lead author rich-text articles in-app; visibility targeting — Everyone/Specific roles/Specific departments/Roles+departments, union semantics; version history with editor + timestamp per revision; read in-app only, no downloads, uploads, search, tags, or favorites), Asset Management (QR/barcode, lifecycle, statuses: Available/Assigned/Repair/Lost/Retired), Employee Requests (Submitted → Manager Approval → HR Approval → Completed), Leave Management (approved by the employee's department head, then HR/Admin), Employee Onboarding/Offboarding Checklists (configurable step templates, auto-created on invite/status-change), Performance Reviews (one current review auto-created per employee at their most recent work-anniversary year — never backfills older missed years; configurable rating/text criteria; the employee's reporting manager or HR/Admin rates and completes it, employee adds an optional self-assessment, HR/Admin finalizes; year-by-year history per employee), Tasks Management (Super Admin/HR-Manager assign to any employee; a department head — the Team Lead role's authority, since there's no separate Teams concept — assigns only within their own department; single assignee per task with title/description/priority/due date/status — To Do/In Progress/Pending/Completed/Cancelled — freely self-updated by the assignee; comment thread; full change history; My Tasks/Assigned Tasks/Team Tasks views; no file attachments in v1), Feedback (incl. anonymous), Employee Directory, Notifications (in-app/email/push), Global Search, Admin Panel.

## Explicitly Excluded from V1
AI Assistant/Chat/Suggestions/Automation/generated content. Storage usage dashboard/quotas/analytics/monitoring. Keep architecture extensible for these later.

## Future Modules (design for, don't build yet)
CRM, Client Portal, Project Management, Attendance, Payroll, Recruitment, OKRs, Internal Chat, Time Tracking, Expense Claims, Finance, SEO Dashboard, Automation Engine, Company Calendar, Training Portal, Rewards & Recognition, Company Wiki.

## Admin Business Management
Originally a six-module roadmap; shrank to **two modules** — Clients & Projects (+ Client Health, folded in) and Payroll — after Automations (2026-08-22) and then Agency Reporting and Finances (2026-08-23) were removed entirely, both per explicit instruction; then grew to **three** on 2026-08-23 with the addition of Leads (module 7 below), a new, explicitly-requested module built the same day, ahead of the standing "CRM is a future module, don't build it yet" rule elsewhere in this file — a direct, explicit override of that default, same as how every other item in this section only gets built once specifically asked for; then grew to **four** on 2026-08-25 with **Financial Reports** (module 8 below) — company-wide revenue/expense/profit reporting, i.e. exactly the thing the 2026-08-23 Finances removal said not to reintroduce "without the user asking again," which is precisely what happened. Clients & Projects and Payroll are shared between Super Admin and HR/Manager (Employees and Team Leads still can't see or touch any of them), enforced via a dedicated permission per module (`clients.manage`, `payroll.manage`) granted to Super Admin implicitly (its "every known permission" grant) and to HR/Manager explicitly in `seed.ts` — never a hardcoded role check, so a custom role could still be granted any of these permissions later without code changes. **Financial Reports and Leads both break that pattern deliberately**: each is Super-Admin-exclusive — Financial Reports since it exposes real company revenue/profit figures, Leads since 2026-08-28 per explicit instruction ("make leads, admin only feature") even though it carries no comparably sensitive figures itself — a materially more restrictive surface than the other two, in Leads' case a deliberate tightening rather than a data-sensitivity call. `main.dart`'s `_superAdminOnlyLabels` set (nav items hidden from HR/Manager even though they're admin-tier) was empty from 2026-08-23 until 2026-08-25 — left in place rather than deleted specifically so a future Super-Admin-exclusive module had somewhere to go without touching `ResponsiveScaffold`'s grouping mechanism — held `{'Financial Reports'}` alone from 2026-08-25 until 2026-08-28, and now holds `{'Financial Reports', 'Leads'}` (see `_hrAndAdminOnlyLabels`, the set holding the other two modules and driving the "HR & Admin Features" heading one tier below "Admin Only Features" and above "General Features" on Super Admin's own sidebar).

**Built one at a time, in this order** (per explicit instruction — do not start the next until the current one meets the Definition of Done):

1. **Clients & Projects** — done. `backend/src/features/clients/`, `frontend/lib/features/clients/`. Client (company + contact info, archivable) and Service (admin-managed catalog, archivable, modeled on `LeaveType`) and Project (client + type One-time/Retainer + status Active/On Hold/Completed/Cancelled + dates + assigned employees/departments/services). **No pricing/payment tracking** — `originalClientPrice`/`deductionRate`/`cost`/`paymentStatus`/`amountPaid` (and the computed `netPrice`/`profit`) were removed entirely 2026-08-23 per explicit instruction ("remove finances from the projects and clientsprojects page"), the same day Finances/Agency Reporting were deleted (see their retired bullets below) — migration `1788600000000-RemoveProjectPricing` drops the columns and `project_payment_status_enum`; `ProjectDetailPage`'s "Pricing" section/"Update Payment" dialog, `ProjectEditorPage`'s pricing fields and live net-price preview, and `ClientsProjectsPage`'s two revenue stat tiles were all removed with it. `Client.archivedAt` is unaffected (a Client Health field, module 2 below). "Teams" on a project means Departments (no separate Teams concept, per this file's RBAC/Departments rule) — a distinct, coarser concept from the project's individually assigned employees. Tasks link to a project via `Task.projectId` (nullable) — reuses the existing Tasks module rather than a parallel task system; a linked task's own assignee visibility/authorization is unchanged. `Settings > Services` manages the service catalog. Nav entry "Clients & Projects", with a "Health" tab (see module 2 below). **Shared with HR/Manager since 2026-08-22** — `clients.manage` is granted to both roles at seed time; this module has no separate `role == 'Super Admin'` nav check, so the permission grant alone controls access. `Client` gained `country` (free text) and `Project` gained 6 free-text SEO-engagement fields 2026-08-23 (migration `1788700000000-AddSeoProjectFields`) when the real SEO client roster was imported: `packageName` (tier label, e.g. "GROWTH +"/"VALUE"/"INTERNAL BRAND - NO LIMIT" — deliberately not an enum, since real packages carry one-off customizations), `backlinksTarget` (a number or "min/max" range, kept as text), `seoSheetName`/`projectFolderName` (labels for an external Google Sheet/Drive folder, not real URLs), and `workingEmailAccount`/`ahrefsAccount` — **reference-only email/username fields, deliberately never paired with a password column**: the user explicitly chose not to store the real account passwords supplied alongside this data (a security call, not an oversight — plaintext credentials in a shared-access ERP table is a real risk); `ProjectDetailPage`'s "SEO Details" section shows a fixed reminder that passwords live in the team's password manager instead. All 24 real client/project pairs from that roster were imported via a one-off script (deleted after running, per this codebase's established convention) with `type: RETAINER`, `name: 'SEO Retainer'`, `assignedEmployees` set to the employee named in the source sheet (matched to real existing `Employee` rows by full name/designation — the sheet used first names only), `targetDepartments: [SEO]`, and `services: [SEO]` (a new Service catalog entry). One employee named in the sheet ("Basit") didn't exist yet; per explicit instruction a new employee "Abdul Basit" (`abdul.basit@zeracreative.com`, SEO Executive, SEO department) was created via the normal `EmployeesService.invite` flow before being assigned his 2 projects. A second, much larger real dataset — a Dec 2024–Aug 2026 sales/deal-tracking log (POC contact, phone, country, lead source, deal-closing date; a "still active" list re-listed every month plus a "One Time Projects" list) — was imported the same day: `Client` gained `leadSource` (free text, migration `1788800000000-AddClientLeadSource`; shown on `ClientDetailPage`'s Contact section and editable on `ClientEditorPage`) to hold it, POC→`primaryContactName`, phone→`primaryContactPhone`, city/country→`country`. Three explicit decisions governed the import (asked via AskUserQuestion given the scale/ambiguity — recorded in `[[project-admin-business-modules-roadmap]]` memory): one `Client` per unique company using its earliest appearance (the monthly re-listing of the same retainer isn't 20 new deals); companies already existing as SEO clients get a new `ONE_TIME` project attached to the existing `Client` rather than a duplicate; and despite being CRM-shaped data (POC/lead-source/deal-stage — CRM itself stays an explicitly unbuilt future module per this file), it was added into Clients & Projects as instructed. Result: 126 new clients + 35 new projects on the 15 pre-existing overlap clients (150 clients / 206 projects total). `ClientsProjectsPage`'s summary row was redesigned the same day (superseding the original 3 status-based tiles) to show **Total Clients**, **Active Clients** (clients with ≥1 `ACTIVE` project — computed client-side from the already-fetched clients+projects lists, no backend change), **Monthly Retainers**, and **One-Time Projects** (the latter two are `Project.type` counts, also client-side).

**Projects/Clients tabs redesigned as spreadsheet-style tables, 2026-08-28** per explicit instruction ("change the layout of projects page and show it as table with more details and do the same for clients tab") — both had been a plain `ListView.separated` of 2-line cards (name + one badge or two). Rebuilt on the exact `_LeadsTable` pattern from the Leads module (`leads_page.dart`): fixed `(label, flex)` columns, a fixed header row, zebra-striped rows, per-cell `Tooltip`s, flex-based `Expanded` cells rather than a horizontally-scrolling `DataTable` (the same header/body-desync reasoning Leads' table was built around) — `_DataTableShell`/`_TableHeader`/`_TableRow` are shared between both tabs within `clients_projects_page.dart`, parameterized by column set and per-cell color/weight callbacks. **Projects** columns: Client, Project, Type, Status (colored by value — green Active/orange On Hold/grey Completed/red Cancelled, matching `ProjectStatusBadge`'s own color mapping, but as plain colored text rather than a pill, to stay dense), Start Date, Package, Employees (assigned employees' names, comma-joined), Services (comma-joined) — Renewal Date and Departments were left off to keep the row from feeling packed to fit inside this page's own `1040`-wide `ConstrainedBox`; both stay one tap away on `ProjectDetailPage`. **Clients** columns: Company, Contact, Email, Phone, Country, Industry, Lead Source, plus a fixed-width trailing `ClientHealthBadge` (a real badge, not text, since health is the one categorical status worth the pill treatment here) — needed a wider trailing-column allowance (160px, up from an initial 110px that overflowed on "Attention Required") to fit the badge's longest label. The Health tab's own short "Needs Attention" list was deliberately left on the old simple `_ClientRow` card — out of scope of what was asked, and a short at-risk list reads better as cards than as a mostly-empty wide table. Existing tests needed re-scoping, not just updating: several assertions (country flag codes, service names, lead sources) had relied on bare `find.text(...)` uniqueness that broke once the same values could legitimately appear twice — once in the "Top N" breakdown panels above the tables, once in a new table cell — fixed by either scoping to the specific `TopBreakdownPanel`/`FormSection` or relaxing to `findsWidgets` where "appears at least once" was the actual thing being tested. Full regression: 357 frontend tests (5 new) + `flutter analyze` clean — no backend changes, so the backend suite was untouched. Live-verified in the browser against the real 152-client/214-project dataset.

**"Top Industries" breakdown panel added, same day** per explicit follow-up ("can you do top industries like top services?") — a 4th panel in `_ClientsBreakdownRow` (`computeTopCounts(clients, (c) => c.industry)`), reusing `AppColors.primary` since each panel's hue is independent, not part of one shared legend (this file's own established reasoning for this row). Adding a 4th panel exposed a real, pre-existing layout gap in the **shared** `TopBreakdownRow` widget (`shared/widgets/top_breakdown_panel.dart`, also used by Leads' 3-panel row): its old logic was a hard "single row above 720px, else stacked column," with no notion of how many panels could actually fit at a given width — 4 panels squeezed into one row inside this page's `1040`-wide container overflowed `_BreakdownBarRow`'s fixed-width label/count columns (caught by the widget-test suite, not by inspection). Rewrote it to compute columns-per-row from `constraints.maxWidth / 220` (220px being `_BreakdownBarRow`'s real minimum before its bar column starts overflowing), wrapping extra panels onto additional rows and padding a short trailing row with invisible `Expanded` spacers so its panels don't stretch wider than the ones above — this generalizes to any panel count without a hardcoded 3-vs-4 special case, and is backward-compatible with Leads' existing 3-panel row (confirmed via its full test suite, unaffected). Same test-scoping issue as the table redesign above recurred and was fixed the same way (`findsWidgets`/panel-scoped) once industry values could legitimately appear in both the new panel and the Clients table's own Industry column. Live-verified against the real dataset: the panel renders correctly (wraps to its own row under the first three, no overflow) but reads "No data yet." — **none of the 152 real clients have `industry` populated** (confirmed via a direct DB count), same reason the table's own Industry column already showed "—" for every row; this is accurate empty-state behavior, not a bug, and will start showing real data once client records get an industry filled in. Full regression: 357 frontend tests + `flutter analyze` clean.

**Project departments derived from assigned employees, same day** per explicit instruction ("in edit project page, no teams selection is needed. assigned employee will already assign the department") — a direct reversal of module 1's original design (a project's departments and its assigned employees were two independently-set fields; see the "Teams (Departments)" note near the top of this bullet). `ProjectEditorPage` no longer has a "Teams (departments)" `_ChipMultiSelect` at all — a new `_departmentsFor(employees, employeeIds)` helper (top-level in `project_editor_page.dart`) collects the distinct `Employee.department` values across whichever employees are currently selected, and that computed set is what actually gets sent as `targetDepartmentIds` on create/update, replacing the old manually-toggled `_selectedDepartmentIds` state entirely. The section is now a plain read-only "Departments" line showing the derived department names (or "Follows whichever employees are assigned above." when none are), updating live as employee chips are toggled — matching the app's established computed-read-only-field convention (`_ComputedUsdField` in Financial Reports). `ProjectDetailPage`'s own "Teams (Departments)" display is unchanged (it was always read-only) and now simply reflects whatever the editor derived. One real consequence worth knowing: **editing and saving an existing project now overwrites its stored `targetDepartments` with whatever the currently-assigned employees' departments compute to** — for the 24 real SEO-roster projects and the sales-log imports, department tags were originally set independently of (though in practice usually matching) the assigned employee's actual department, so a project whose manually-set department doesn't match its employee's real department will silently correct itself the next time anyone saves that project. Live-verified against a real project (Vip Luxury Travel Advisor's "SEO Retainer", assigned to Babar Hussain) — the Departments line correctly read "SEO", matching his real department, with no separate picker shown. Full regression: 358 frontend tests (2 new/changed in the editor's own suite) + `flutter analyze` clean — no backend changes, so the backend suite was untouched.

**SEO-specific fields dropped down to just "Package"; end date and the header restyled, same day** per explicit follow-up — a real bug report ("why it is saying seo details in smm project?") plus three simplification asks in one message. The bug: `ProjectDetailPage`'s "SEO Details" section title was hardcoded regardless of the project's actual `services` — any project with at least one of `packageName`/`backlinksTarget`/`seoSheetName`/`projectFolderName`/`workingEmailAccount`/`ahrefsAccount` set got the "SEO Details" label even if it was an SMM (or any other) engagement, since those fields were never actually SEO-exclusive at the data level, just at the label level. Fix: `backlinksTarget`/`seoSheetName`/`projectFolderName`/`workingEmailAccount`/`ahrefsAccount` were dropped from both `ProjectDetailPage`'s display and `ProjectEditorPage`'s inputs entirely per explicit instruction ("No hardcoded fields are needed.. just the one project details no need of backlink target sheet folder working email ahref account etc.") — only `packageName` remains, now under a plain "Package" section title (no "SEO" framing at all). **Deliberately UI-only, not a backend/schema removal**: `Project`'s entity/columns/DTOs still carry all 6 fields untouched, so the real, previously-imported SEO-roster data (working email/Ahrefs account references, backlink targets, sheet/folder names) is neither deleted nor destroyed — it simply isn't shown or editable through the app anymore. This was a deliberate, autonomous call (not asked in the message) given the real risk of permanently destroying real reference data via a full-stack removal on a live production system; a UI-level removal fully satisfies "no hardcoded fields are needed" without that risk, and is easily extended to a real backend removal later if that's what's actually wanted. `endDate` ("due date") was removed the same way, same reasoning — dropped from the editor's Start/End date row (now just a single "Start date" field) and from the detail page's Start/Renews summary line, backend column untouched. Finally, the header ("need project name is bold in big and smm retainer in small under it") — `project.name` moved from `headlineSmall` to `headlineMedium` with explicit `fontWeight: FontWeight.bold`, with `project.clientName` unchanged as the smaller secondary-colored subtitle directly underneath (this pairing already existed; the ask was to make the name unambiguously bold and bigger, not to restructure what's shown). Live-verified against two real SMM projects (Zenna Beds, Gen6 Intelligence) — bold/large name, small client subtitle, no end date, and (via a dedicated new test, since neither of these two real projects had a package set) confirmed a project with an SMM service and legacy `backlinksTarget`/`seoSheetName`/etc. values shows a plain "Package" section with none of the other fields, never "SEO Details". Full regression: 361 frontend tests (7 new/changed across the detail and editor test files) + `flutter analyze` clean — no backend changes, so the backend suite was untouched.
2. **Client Health** — done, folded into the Clients & Projects module rather than a separate one (same controller/permission surface). `Client` gained `healthStatus` (Healthy/Attention Required/At Risk, default Healthy), `healthFactors` (a fixed 6-item checklist — payment/performance/delays/complaints/communication/renewal — toggled per update, not individually rated), and `healthNotes` — all set only via an explicit `POST /clients/:id/health`, never inferred/computed. Each update also writes an immutable `ClientHealthHistory` row (previous/new status, factors, notes, actor snapshot via the existing `resolveActorName` util) — same convention as `TaskAuditLog`. `GET /clients/health-summary` gives counts by status for the dashboard. Frontend: a "Health" FormSection + history list on `ClientDetailPage` with an "Update Health" dialog, a health badge on every client row, and a third "Health" tab on `ClientsProjectsPage` (stat tiles + a worst-first At Risk/Attention Required list, Healthy clients excluded). All gated by the existing `clients.manage` permission — no new permission needed.
3. ~~**Agency Reporting**~~ — built 2026-08-21, **removed entirely 2026-08-23** per explicit instruction ("remove all finances from the system.. keep the clients and projects and tasks.. No finance or finance reporting is needed"). It introduced no schema of its own (only read existing Client/Project fields), so removal was clean: deleted `backend/src/features/agency-reporting/`, `frontend/lib/features/agency_reporting/`, the `reports.view` permission, and the nav entry — see git history for the original design (a `GET /agency-reporting/report?from=&to=` P&L-style endpoint with date-range presets, a compare-to-previous-period toggle, top-clients-by-profit, projects-by-status) if ever needed again.
4. ~~**Finances**~~ — built 2026-08-21, **removed entirely 2026-08-23** in the same instruction as Agency Reporting above. Deleted `backend/src/features/finances/`, `frontend/lib/features/finances/`, the `finances.manage` permission, the `expenses`/`expense_category_enum` schema (migration `1788500000000-RemoveFinancesAndAgencyReporting`), and the nav entry. (One day earlier, 2026-08-22, its revenue/cost/profit tiles had already been dropped as duplicates of Agency Reporting's — see git history for that intermediate state; both modules are gone now regardless.) The Admin Dashboard's "Admin Business Management" stats section lost its `_NetProfitCard`/`_OutstandingInvoicesCard` tiles (sourced from these two modules) but keeps `_ActiveProjectsCard`/`_ClientsAtRiskCard` (Clients & Projects) and `_LatestPayrollRunCard` (Payroll).
5. **Automations** — built, then removed 2026-08-22 per explicit instruction ("remove automations feature and automations.. make sure everyone is notified for important stuff"). It originally shipped as a **fixed catalog** of 3 admin-toggleable automation types (Project Renewal Reminder, Task Deadline Reminder, Annual Leave Reset) plus an `AutomationExecutionHistory` audit table — see git history (`backend/src/features/automations/`, `frontend/lib/features/automations/`, migration `1788100000000-AddAutomations`) for the original design if ever needed again. It introduced the app's **first persisted `Notification` entity** (`backend/src/features/notifications/`, a core module, not gated by any Admin Business Management permission — every route only ever touches the caller's own notifications), which survives the removal and is now populated directly by each feature module instead of through a generic admin-toggleable layer:
   - **Tasks** (`TasksService.handleDailyDeadlineReminderCheck`, `@Cron(EVERY_DAY_AT_2AM)`): notifies a task's own assignee unconditionally when its `dueDate` is 7 days out. Idempotent via `Task.lastDeadlineReminderSentFor`.
   - **Leave** (`LeaveService.handleDailyAnnualResetCheck`, `@Cron(EVERY_DAY_AT_3AM)`): runs the annual reset automatically once due (idempotent via the pre-existing `getResetStatus`), then notifies **every active employee** directly that their balance was reset, plus every `leave.manage` holder with the run's summary count — a broader audience than the original admin-only notice, since the reset affects every employee personally.
   - **Payroll** (`PayrollService.payRun`): notifies every employee with a line item in the run when it's marked Paid — no link, since there's no employee-facing payslip view (see module 6 below).
   - **Performance Reviews** (`PerformanceReviewsService.runDueCheck`, existing `@Cron(EVERY_DAY_AT_1AM)`): notifies the employee directly ("your review has been created") and whoever needs to act on it — their reporting manager, or every `performance.manage` holder if they have none set.
   - **Project Renewal Reminder has no replacement** — it only ever notified `clients.manage` holders (not "everyone"), so it didn't fit the replacement goal; `ClientsService.getProjectsNeedingRenewalReminder`/`markRenewalReminderSent` and `Project.lastRenewalReminderSentFor` were deleted along with it (migration `1788300000000-RemoveAutomations`). `renewalDate` itself is kept — it's still a legitimate project field, just no longer watched by anything.
   - Frontend: `NotificationBell`'s existing "Reminders" section (`GET /notifications?unreadOnly=true`) is unchanged and keeps working since it's agnostic to which module created a given notification; a `PERFORMANCE_REVIEWS` `NotificationLinkTarget` value was added (migration `1788400000000-AddPerformanceReviewNotificationLinkTarget`) for the new review notifications' deep-link. No nav entry, no settings page, no permission — this is no longer a Super Admin module at all.
6. **Payroll** — done. `backend/src/features/payroll/`, `frontend/lib/features/payroll/`. A new, distinct `payroll.manage` permission. Reuses the pre-existing `SalaryRecord` entity (base salary + effective-dated history, already built for the employee profile's Salary History screen) rather than duplicating it — Payroll's own new pieces are a **formal, idempotent Payroll Run** (`PayrollRun`: month/year with a `UQ_payroll_runs_month_year` uniqueness constraint, status Draft → Finalized → Paid) and one `PayrollLineItem` per active employee per run (`baseSalary` snapshotted at generation time via a new `EmployeesService.getSalaryAsOf(employeeId, asOfIsoDate)` — the latest `SalaryRecord` whose `effectiveDate` is on or before the run's month-end, plain string comparison since dates are already 'YYYY-MM-DD' — plus one-off `allowances`/`overtime`/`deductions`/`advances`/`tax`/`fines`, all editable only while the run is Draft; `netPay` computed in the mapper, never stored). Deliberately **no recurring-item concept** (every adjustment is entered fresh per run) and **no formal loan ledger for advances** (a one-time entry, no balance/repayment automation) — both scoped down from the roadmap's original wording after confirming with the user, mirroring Automations' "fixed catalog over generic engine" restraint. `POST /payroll/runs` explicitly 409s on a duplicate month/year (an admin-triggered action, so a loud conflict beats silent dedup) rather than the daily-cron idempotency pattern used elsewhere. A Finalized/Paid run's line items are locked — the "permanent payment history" the roadmap named. No employee-facing payslip view — no self-service surface in V1 regardless of who can access the module. **Shared with HR/Manager since 2026-08-22** — `payroll.manage` is granted to both roles at seed time. Frontend: `PayrollPage` (run list + "Generate Payroll" month/year dialog) and `PayrollRunDetailPage` (run summary, Finalize/Mark Paid actions gated by status, a `DataTable` of line items — tapping a row while Draft opens an edit dialog for that employee's adjustment figures). Nav entry "Payroll" — coincides in name with the pre-existing Admin Dashboard's own "Payroll" stats section heading (`_SectionHeader('Payroll')`, unrelated), which is fine on-screen but meant two existing widget tests had to stop asserting on bare `find.text('Payroll')` in favor of the already-present, still-unique `'Monthly Payroll'` label. **`bonuses` removed, `fines`/`lateCount` added, 2026-08-26** per explicit instruction ("get rid of bonuses and add option to deduct late comings (3 lates = 1 unpaid off) and also add option to deduct fines") — migration `1789300000000-UpdatePayrollLineItems`. `fines` is a plain one-off amount, deliberately separate from the generic `deductions` field so fines stay distinguishable in the line-item table. `lateCount` is a plain entered count (no attendance-tracking module exists yet — Attendance is still a future module per this file) rather than derived from any real attendance data; the mapper computes `lateDeductionRs = floor(lateCount / 3) * (baseSalary / daysInMonth(run.year, run.month))` on every read, never stored, same "computed not persisted" convention as `netPay` — `netPay` itself now also subtracts `fines` and `lateDeductionRs`. **Superseded the same day** by a full second rework after the user pasted their real payroll spreadsheet (33 real employees, exact deduction/addition columns) with the instruction to match it exactly — every formula was reverse-engineered against every row of the real data via a verification script before any code changed, given real pay was at stake. Two convention changes from the first version above: `DAYS_PER_MONTH = 30`/`HOURS_PER_DAY = 8` are now **fixed constants**, not `daysInMonth(run.year, run.month)` — the real spreadsheet divides by a flat 30 regardless of the actual calendar month, so the mapper's `toPayrollLineItemResponse` no longer takes a `run` parameter at all. `lateCount` was renamed `lateDays` (same 3-for-1 unpaid-day rule, now against the flat daily rate) and four fields were added: `reimbursement`/`commissions` (plain one-off additions), `totalAbsent` (→ `absentDeductionRs = totalAbsent * dailyRate`), `lateHours` (→ `lateHoursDeductionRs = lateHours * hourlyRate`) — migration `1789400000000-AddPayrollAttendanceAndPieceRateFields`. The one genuinely ambiguous piece — a subset of employees paid `Quantity × Per-amount` instead of a fixed salary (confirmed via exact multiplication match against 5 real rows) — was put to the user via AskUserQuestion ("Manual entry per run" vs "Build Quantity/Rate as real fields"); **the user chose the more complex option**, so `PayrollLineItem` gained real `quantity`/`perUnitRate` columns, and the mapper's effective `baseSalary` becomes `quantity * perUnitRate` when both are set on a line item, else the `SalaryRecord` snapshot — computed on every read, never overwriting the immutable snapshot column. Frontend: the line-items `DataTable` grew to 19 columns (Base/Qty/Rate/Allowances/Overtime/Reimbursement/Commissions/Deductions/Fines/Absent/Absent Ded./Late Hrs/Late Hrs Ded./Late Days/Late Days Ded./Advances/Tax/Net Pay) and the edit dialog was reorganized into 4 labeled sections — "Piece-rate pay (optional)", "Additions", "Attendance deductions", "Other deductions" — each with inline helper text explaining the flat-30 and 3-for-1 rules.

**Freelancers added 2026-08-26, same day** — while reconciling the real July spreadsheet above, the user identified two of the "missing employee" rows (Hamza Saqib, Kulsum Zehra — both paid `quantity × per-amount`) as freelancers, not employees, and asked for a first-class way to add freelancers and pay them a variable monthly amount, plus a freelancer headcount stat "same as employees." Built as a new, deliberately lightweight standalone module `backend/src/features/freelancers/` (`Freelancer`: `fullName`, free-text `role`, `notes`, `isActive` — no department/designation/company-email, since freelancers never get an ERP login) rather than folding into Employee Management, mirroring this file's established "flat standalone entity, no relation to the heavier module" pattern (Leads vs. Client, Service vs. Project). Reuses `payroll.manage` — no new permission — since freelancer management only exists to serve Payroll.

Payroll integration keeps one unified `PayrollRun`/`PayrollLineItem` model rather than a parallel freelancer-payment concept, since the real spreadsheet lists freelancers in the exact same table with the exact same Total Net Salary math: `PayrollLineItem.employeeId` was made nullable and a nullable `freelancerId` FK added (migration `1789500000000-AddFreelancers`) — exactly one of the two is set per line item, enforced in `PayrollService`, not a DB constraint (mirrors `employeeId`'s own original lack of an FK constraint on this table). Unlike active employees, freelancers are **never auto-included when a run is generated** — a freelancer might not work every month, so an admin adds them to a specific run via a new `POST /payroll/runs/:runId/freelancer-line-items` (`AddFreelancerLineItemDto`: `freelancerId` + `baseSalary` entered directly + optional notes). Since a freelancer has no `SalaryRecord` to snapshot from, `baseSalary` itself becomes directly editable post-creation for a freelancer's line item only — `UpdatePayrollLineItemDto` gained an optional `baseSalary` field that `PayrollService.updateLineItem` rejects with a 400 if attempted on an employee's line item (protecting the immutable-snapshot invariant everywhere else). The mapper resolves `employeeName`/`employeePhotoUrl` from whichever of `employee`/`freelancer` is set and adds an `isFreelancer` flag to the response so the frontend knows which UI to show; `PayrollService.payRun`'s per-line-item notification loop skips freelancer line items (no `User`/login account to notify). The existing piece-rate `quantity`/`perUnitRate` override, and the full deductions/additions shape, are left available to freelancer line items unchanged — Hamza Saqib and Kulsum Zehra are both piece-rate freelancers, so the mechanism needed to keep working identically for them.

One real bug caught by this session's own "explicit `type` on nullable columns" lesson (documented in the first payroll rework's automations-era predecessor bug, see [[project-admin-business-modules-roadmap]]) recurring on the very next nullable columns added afterward: `Freelancer.role?: string | null` and the new `PayrollLineItem.employeeId?`/`freelancerId?: string | null` all crashed the backend on startup with `DataTypeNotSupportedError: Data type "Object"` until each got an explicit `@Column({ type: 'varchar' | 'uuid', nullable: true })` — reflect-metadata still can't infer a real Postgres type from a `X | null` union. Frontend: `frontend/lib/features/freelancers/` (full layered stack, no UI beyond what's described below), `PayrollPage` converted from a flat run list into a `DefaultTabController` with "Runs" and "Freelancers" tabs (roster list + Active/Inactive toggle + an Add/Edit dialog), `PayrollRunDetailPage` gained an "Add Freelancer" action (`FormSection` gained an optional `trailing` slot for this) and a "Freelancer" badge on freelancer rows in the line-items table; the edit dialog shows a directly-editable "This month's pay" field instead of the read-only "Salary snapshot" line when editing a freelancer's line item. Admin Dashboard's Admin Business Management section gained a `_TotalFreelancersCard` (active-freelancer count) alongside `_LatestPayrollRunCard`. Full regression: 356 backend tests (27 new) + `nest build` clean, 329 frontend tests (13 new) + `flutter analyze` clean. Live-verified end-to-end via the real backend (curl for freelancer creation and adding one to the real July run — the live browser's text-input automation was flaky for this specific dialog, a known environment limitation from earlier in this session, not a code defect, and the identical code path is covered by the passing widget-test suite using Flutter's own text-input simulation): Hamza Saqib's and Kulsum Zehra's real July amounts (27,300 and 5,000, matching their real 455×60 and 5×1000 piece-rate figures) were added to the actual July 2026 run, confirmed via the Freelancers tab, the run's line-items table (Base + "Freelancer" badge), the run list's total, and the Dashboard's "Total Freelancers: 2" tile all updating correctly and consistently.

**Radically simplified back down, 2026-08-26, same day** — per explicit instruction ("Make payroll little simpler. get rid of all the deductions, fines, absents etc. I want just employee name, base salary, net salary paid"), superseding essentially all of the deduction/attendance machinery built earlier the same day (the flat-30-day math, `reimbursement`/`commissions`/`totalAbsent`/`lateHours`/`lateDays`/`fines`/`deductions`/`advances`/`tax`/`allowances`/`overtime` — all removed). `PayrollLineItem` now carries only `baseSalary` (still an immutable per-employee SalaryRecord snapshot, or directly-entered for freelancers, unchanged), the optional piece-rate `quantity`/`perUnitRate` pair (kept deliberately — not a deduction, it's how Kulsum Zehra/Hamza Saqib/other real piece-rate employees' base pay is actually computed, confirmed against the real spreadsheet earlier the same day), and a new `netPay` — a **plain, directly-entered, stored** figure (no longer computed from a formula) that defaults to `baseSalary` when a line item is created and is then freely editable for every line item, not just freelancers'. Migration `1789600000000-SimplifyPayrollLineItems` backfills `netPay = ` the old full formula before dropping the 11 now-gone columns. Frontend: the line-items `DataTable` shrank from 19 columns back to 3 (Employee/Base/Net Pay); the edit dialog dropped its Additions/Attendance-deductions/Other-deductions sections, keeping only the (freelancer-only) Amount field, the Piece-rate pay section, and a new plain "Net Pay" field. Full regression: 351 backend tests + `nest build` clean, 327 frontend tests + `flutter analyze` clean.

This migration hit a real, live-data close call worth remembering generally: **this dev database has never actually gone through `migration:run`** — every schema change all session has landed via `synchronize: true` picking up entity edits automatically, so the `migrations` tracking table doesn't reflect reality and running `npm run migration:run` here fails immediately on the *first* migration file (`type already exists`, since that migration's `CREATE TYPE` was already applied by `synchronize` months ago, just never recorded). Worse: `synchronize: true` tried to add the new `netPay` column as `NOT NULL` directly against the real, already-populated July 2026 payroll run (27 live rows) and crashed the backend in a retry loop — Postgres correctly refused (`column "netPay" ... contains null values`) rather than silently corrupting data, and the failed `ALTER TABLE` was transactional and rolled back cleanly (confirmed via `\d payroll_line_items` showing the old schema fully intact before proceeding). Recovered by running this migration's exact `up()` SQL directly via `psql` — add `netPay` nullable first, `UPDATE` it from the old formula, `SET NOT NULL`, *then* drop the old columns — the same backfill-before-constrain pattern the migration file itself uses, just executed by hand since `migration:run` wasn't usable here. **Takeaway: in this dev environment, never trust `synchronize: true` to safely evolve a column from nullable-computed to `NOT NULL` (or any schema change needing a data backfill) on a table with real rows — write and manually execute the raw SQL (add nullable → backfill → constrain → drop) before letting the app reconnect, exactly like a real migration would.**

7. **Leads** — done. `backend/src/features/leads/`, `frontend/lib/features/leads/`. A flat, standalone `Lead` entity (no relations to Client/Project/Service — a prospect only becomes a `Client` manually, later, outside this module) with fields `leadDate` (date, required), `fullName` (required), and free-text `companyName`/`leadSource`/`phone`/`email`/`country`/`remarks`/`serviceInterested` (all nullable). `serviceInterested` is deliberately free text rather than a `Service` catalog relation, same reasoning as `Client.leadSource`/`Project.packageName` elsewhere in this file — a lead's interest is often vague, spans multiple services, or predates a matching catalog entry, and data was going to be bulk-imported right after the module shipped so nothing could block on catalog completeness. A new, distinct `leads.manage` permission (not reused from `clients.manage`, since a lead isn't a client yet and the roadmap's one-module-at-a-time rule treats this as its own thing) — **shared with HR/Manager from day one**, same pattern as the other two modules. Backend mirrors `Service`'s CRUD shape (`getLeads`/`createLead`/`updateLead`); phone numbers are normalized to always carry a leading `+` (`LeadsService`'s `normalizePhone`) and a blank string sent by the editor on update clears a field to NULL rather than being stored literally (`blankToUndefined` — the editor's own update path deliberately still sends plain `.text.trim()`, since the datasource's null-aware map syntax already omits untouched fields; the "blank means clear" translation happens backend-side only). 2,154 real leads were imported the same day; **archiving was removed 2026-08-24** per explicit instruction ("No need of archive stat and feature in leads") — `isArchived` was dropped (migration `1789000000000-RemoveLeadArchiving`) and the list rebuilt as a fixed-column, Excel-style spreadsheet table (`_LeadsTable`, zebra striping, per-cell tooltips, virtualized `ListView.builder`) instead of a card list. Frontend: nav entry "Leads" (own full page, not nested under Settings) with stat tiles, a monthly-volume bar chart, three "Top N" breakdown panels (countries/services/lead sources — `Client.country` values render as short codes, e.g. "PK"/"UAE"/"KSA", via a case-insensitive lookup table built from the real imported data, falling back to the original text for anything unrecognized), the spreadsheet table, and a `LeadEditorPage` full-page create/edit form. Migration `1788900000000-AddLeads`.

**Iterated 2026-08-26**, several small follow-ups in one day: a redundant body heading ("Leads" appeared both in the top bar and as a page heading — the only top-level nav page with this bug, confirmed by checking every other one) was removed, matching the established Financial Reports convention of button-only page headers; the monthly volume chart (`_MonthlyLeadsChart`) now labels every bar directly with its count (not hover-only) and orders newest month first/oldest trailing right, so recent activity is visible without scrolling; `_LeadsTable` gained client-side pagination at 50 rows/page (the backend's `GET /leads` already sorts newest-first, so page 1 is naturally the latest 50, no backend change needed); the page section order changed to stats → breakdown panels (Top Countries/Lead Sources) → monthly chart → table (reordered again 2026-08-30 to stats → monthly chart → breakdown panels → table, per explicit instruction to move the monthly chart up right after stats); the stats row gained Total Countries/Total Lead Sources tiles (a Total Services tile was added then removed same day, per explicit instruction) computed via a new `_countDistinct` helper using the same selectors as the breakdown panels, for consistency. A bulk **import feature** was added the same day ("need import feature in leads") — designed autonomously since the clarifying `AskUserQuestion` (file format/column-mapping/dedup) went unanswered, per [[feedback-autonomous-progression]]: paste-based (a tab-separated textarea, no file upload/new package dependency), fixed column order matching the table's own columns (Date/Full Name/Company/Phone/Email/Country/Service Interested/Lead Source/Remarks), and always-create with no deduplication (matches how every real data batch has been imported all session). Backend: `POST /leads/import` (`ImportLeadsDto` — a `class-validator` nested array of `CreateLeadDto`, rejects the whole batch on any single invalid row per this app's global `ValidationPipe`, which is why all row-level validation/preview lives client-side instead), `LeadsService.importLeads` reusing a new `buildLead` helper extracted from `createLead` so normalization (phone `+`-prefixing, blank-to-undefined) stays identical between single-create and bulk-create, and a new `LeadRepository.saveMany` (`repository.save()` on an array, mirroring `PayrollLineItemRepository.saveMany`'s precedent). Frontend: `LeadImportPage` — a paste `TextField`, a parser (`parseImportText`) trying `DateTime.tryParse` then a hand-rolled "MMM D, YYYY" fallback (matching the format this session's own real-data import scripts used), a live preview listing each row as ready/errored, and an "Import N Leads" button sending only the valid rows in one request; wired in via a new "Import Leads" button next to "New Lead" on `LeadsPage`.

**Also 2026-08-26**: fixed a `TabBar`-with-`isScrollable: true` default leading-inset gap (Flutter's default behavior, not a bug in this app's code) across all 3 scrollable tab bars in the app (Payroll, Clients & Projects, Tasks) via `tabAlignment: TabAlignment.start`.

**Made Super-Admin-exclusive, 2026-08-28** per explicit instruction ("make leads, admin only feature") — Leads had been shared with HR/Manager since its 2026-08-23 launch (see the section intro above); this reassigns it to the same tier as Financial Reports rather than changing anything about the module itself. Backend: `leads.manage` removed from HR/Manager's permission array in `seed.ts` (Super Admin keeps it via the implicit "every known permission" grant; `LeadsController`'s `@Permissions('leads.manage')` class-level guard is unchanged, since the fix is in who holds the permission, not the check itself). Frontend: `'Leads'` moved from `main.dart`'s `_hrAndAdminOnlyLabels` set into `_superAdminOnlyLabels`, so it now renders in the "Admin Only Features" sidebar group instead of "HR & Admin Features" and disappears from HR/Manager's nav entirely; `LeadsPage`'s own `hasPermission('leads.manage')` gate (unchanged code) then naturally denies direct navigation too. Re-seeding a dev/prod DB after this change only updates the stored role-permission rows — an HR/Manager user already signed in keeps the old permission until they sign out and back in, since it's embedded in their JWT at issue time (see [[project-dev-db-never-migrated]]-adjacent JWT-staleness gotcha noted elsewhere in this file's history). Added a frontend test to `leads_page_test.dart` asserting an HR/Manager viewer (with `clients.manage`/`payroll.manage` but not `leads.manage`) now gets the same `AccessDeniedView` an Employee already got — mirroring the precedent test in `financial_reports_page_test.dart` for that module's own HR/Manager exclusion.

**"Last Lead Received" stat tile added 2026-08-29** per explicit follow-up ("in stats, can you show when was last lead received?") — `_LeadsStatsRow` gained a new `MetricCard` (between Total Leads and New This Week) showing the most recent `leadDate` across all loaded leads, formatted via the shared `formatDisplayDate` util (e.g. "Aug 28, 2026"), or "—" when there are no leads. A new `_lastLeadReceivedIso` helper finds the max `leadDate` directly by scanning every lead rather than trusting `leads.first` — `GET /leads`'s current `ORDER BY leadDate DESC, createdAt DESC` makes that equivalent today, but the stat's own correctness shouldn't silently depend on the backend's sort order never changing. Frontend-only change (no backend calls involved), 2 new/changed assertions in `leads_page_test.dart`.

**Import duplicate detection added 2026-08-30** per explicit follow-up ("does import data have capability to ignore duplicated data?" → "yes") — reverses the bulk-import feature's original always-create, no-dedup design (see the 2026-08-26 entry above) now that the module is past its one-off historical-batch-import phase and reads more like an ongoing data-entry tool, where the same contact really can get pasted in twice. Deliberately a **client-side-only, soft/informational check**, not a backend constraint — unlike Financial Reports' year/month `@Unique`, a repeat lead is a fuzzy business signal (the same person re-enquiring, or two different people sharing a household phone), not a data-integrity violation, so a hard uniqueness constraint would be the wrong tool here; this mirrors Financial Reports' own client-side duplicate-month warning in spirit (flag before submit, using already-loaded data) but not in strictness (that one also has a backend 409 as a hard backstop — this one doesn't, on purpose).

A new pure function, `flagDuplicates(rows, existingLeads)` in `lead_import_page.dart`, runs over the already-parsed rows every time `_parsed` or the loaded `leadsListProvider` data changes (recomputed directly in `build()`, not cached in state, so it always reflects the current lead list): a row is flagged when its phone or email (normalized — phone compared by digits-only so formatting differences like "+1 555-0100" vs "15550100" don't hide a match; email compared case-insensitively) matches either an existing lead already in the system or an earlier row in the same paste. A row with neither phone nor email can't be checked and is never flagged — there's nothing reliable to key off. Existing-lead matches are checked before within-batch matches so the reported reason always names the more useful match when both would apply.

Flagged rows are **excluded from the import by default** (answering the "ignore duplicated data" question directly) but not silently dropped — each renders in the preview as a `CheckboxListTile` (orange duplicate-copy icon, the specific reason e.g. `Same phone as existing lead "Jane Existing"`, and a live "will be imported"/"skipped" suffix) that the user can check to include that one row anyway; unrelated rows are unaffected by any single toggle. The preview's summary line grew a third clause (`N possible duplicates (excluded by default)`) alongside the existing ready-to-import/errors-skipped counts, and the instructions text above the paste box explains the behavior up front. No backend changes — `POST /leads/import` and `ImportLeadsDto` are untouched, since the decision of what to submit is made entirely in the preview before the request goes out. 7 new/changed tests (6 pure-function `flagDuplicates` cases + 1 full widget-level flow: paste a phone-matching row → excluded by default → check it → included and actually sent). Full regression: 370 frontend tests (7 new) + `flutter analyze` clean — backend suite untouched, no backend changes were made.
8. **Financial Reports** — done, 2026-08-25. `backend/src/features/financial-reports/`, `frontend/lib/features/financial_reports/`. A flat, standalone `FinancialRecord` entity, one row per plain calendar month (`year`, `month` 1-12, `@Unique(['year', 'month'])`) — deliberately **not** grouped by fiscal year even though the source spreadsheet was, per explicit instruction ("No need of fiscal year, you can do normal calender"). Stores only raw monthly inputs — `revenueRs`/`revenueUsd`/`expenseRs`/`expenseUsd`/`fxRate` (PKR-per-USD, reference only, entered directly rather than derived from it since real months don't convert at one clean rate) — and computes `profitRs`/`profitUsd`/`profitPercent` in the mapper on every read, never as stored columns, the same convention as `PayrollLineItem.netPay`. A new, distinct `finances.manage` permission, deliberately **Super-Admin-exclusive** (see the section intro above) rather than reusing the shared-with-HR/Manager pattern of every other module here — real revenue/profit data warranted a stricter boundary, decided without asking since `_superAdminOnlyLabels` was already sitting empty and documented as built for exactly this. 42 months of real data (Jul 2022 - Dec 2025) were imported via a one-off script (deleted after running); each source row's month label already carried its own real calendar year (e.g. "Jul 2022"), so the import needed no fiscal-year math — every figure was transcribed directly. `POST /financial-records` 409s on a duplicate year/month, mirroring Payroll's `POST /payroll/runs` pattern. No in-app editor in v1 — this was a pure reporting surface at launch; new months were entered the same way the initial import was, since only "see my monthly and yearly reports in stats and graphs" was asked for at the time (an in-app editor was added later — see the 2026-08-27 update below). Frontend: nav entry "Financial Reports" under a new "Admin Only Features" sidebar group (Super Admin only); a year selector (`SegmentedButton`); stat tiles (Total Revenue/Expense/Profit, Profit Margin, each PKR-primary with a USD secondary line via `MetricCard`'s existing `secondaryValue` slot); a monthly Revenue-vs-Expense grouped bar chart (2 fixed categorical hues: primary=Revenue, secondary=Expense, with a legend); a monthly Profit/Loss chart (single series, bar color signals polarity — green=profit, red=loss — status-color treatment rather than a diverging gradient, per this app's `dataviz` skill guidance); a Revenue-vs-Expense-by-year chart across every calendar year present, independent of the year selector; and a monthly detail `DataTable`. No charting package dependency, consistent with every other chart in this app (Leads' monthly chart, breakdown panels) — hand-rolled from plain Flutter widgets with `Tooltip` as the hover layer. **Revenue Growth — All-Time line chart added 2026-08-26** per explicit instruction ("linear line graph showing revenue growth from day 1") — a single-series straight-line chart (hand-rolled `CustomPainter`, not a bar chart) plotting every record's revenue oldest-to-newest, always the full history regardless of the Period selector above it (unlike the two monthly charts, which stay year-scoped). Scaled between the data's own min/max rather than from zero, so the trend's slope stays visible instead of flattening near the top of a zero-based axis. With 40+ months of history, a value label on every point would be unreadable, so per the `dataviz` skill's line-chart guidance it relies on a hover tooltip (via `Tooltip` per point) rather than direct labels; year ticks appear only at each January (plus the very first point, left-aligned rather than centered so it can't clip off the scroll area's left edge). **Redesigned 2026-08-26 (same day, later) to remove scrolling** per explicit instruction ("revenue growth all time growth should not be scrollable. it should show all data in one screen. all the dots should show revenue.") — replaced the fixed-pixel-spacing-inside-a-horizontal-`SingleChildScrollView` layout with a `LayoutBuilder` that computes point spacing from the available width (`plotWidth / (records.length - 1)`), so all ~42 months render on screen at once with no scroll needed; the year-label right-anchoring logic was extended to the last point (mirroring the existing first-point left-anchor) so neither edge label clips.

**In-app editor added 2026-08-27** per explicit follow-up ("how can I add new financial data? there is no add feature") — the backend had shipped `POST`/`PATCH /financial-records` from day one (409s on a duplicate year/month, an update-DTO with every field optional), but nothing on the frontend ever called them; new months could only be entered via a one-off import script. Added `FinancialRecordEditorPage` (mirrors `LeadEditorPage`'s create/edit-in-one-page shape): a month dropdown + year field, then Revenue/Expense in both Rs and USD plus the reference FX rate — all five money fields are sent to the backend as numeric **strings**, not JSON numbers, since `FinancialRecord`'s columns are TypeORM `numeric` (mapped to `string` in the entity) and the DTOs validate with `@IsNumberString()`; whatever the user typed is passed through once confirmed to parse, no reformatting. An "Add Record" button was added both in the normal page view and in the empty state (previously a dead end — no records meant no way to create the first one in-app either), and every row in the Monthly Detail table is now tap-to-edit (`DataRow.onSelectChanged`, with `showCheckboxColumn: false` so the table doesn't grow an unwanted selection-checkbox column just from making rows tappable). The duplicate-month conflict surfaces the backend's own message text verbatim (`"A financial record for M/YYYY already exists."`), matching this app's established 409-handling convention (`PayrollRepositoryImpl`, `RoleRepositoryImpl`, etc.). Full regression: 349 frontend tests (8 new) + `flutter analyze` clean — no backend changes were needed at all, so the backend suite was untouched.

**Proactive duplicate-month warning added same day**, immediately after, once live use surfaced the gap: the 409 only fired *after* filling in every field and hitting Create, which read as "it let me add a record for a month that's already there" even though the backend had correctly refused it every time (verified against the real DB — no duplicate was ever actually created). `FinancialRecordEditorPage` now watches `financialRecordsListProvider` directly and computes which months are already on file for the typed year (excluding the record's own id when editing, so editing a record never flags itself); a taken month renders as a disabled `DropdownMenuItem` labeled "(already added)", an inline warning appears below the Month/Year row naming the exact conflicting month, and the Create/Save button disables outright — all live as the user types the year or changes the month, no submission needed to find out. This sits **on top of** the existing 409 check, not instead of it (a race between two admins is still caught server-side) — deliberately not the same "reactive-only" choice CLAUDE.md's Payroll bullet documents for `POST /payroll/runs` ("a loud conflict beats silent dedup"), since re-typing five money fields just to get rejected is worse UX for a form this size than it is for Payroll's single-click "Generate Payroll" action.

**Editor redesigned 2026-08-28** per explicit instruction ("USD field should not be editable... it should show organically through multiplying the FX rate... no month edit field. No year selection field. no usd field [in edit mode]"). Two changes: (1) Revenue/Expense **USD are no longer typed** — the editable USD `TextFormField`s were removed entirely, replaced by a read-only `_ComputedUsdField` (an `InputDecorator`, matching the field-styled-but-non-interactive convention `LeadEditorPage`'s Date field already used) showing `round(Rs ÷ FX rate)`, recomputed live via `TextEditingController` listeners on the Rs and FX-rate fields — verified against the real data that this is the actual relationship (e.g. the real Jan 2023 row: 604657 Rs ÷ 230 FX rate = 2629.37 → 2629, the exact stored USD), not an approximation. The FX Rate field moved to the top of the form (above Revenue/Expense) since the computed fields now depend on it, and its helper text was rewritten from "reference only, not derived from this rate" to "used to compute the USD figures below automatically" — a direct reversal of the original 2026-08-25 design note. A new `_fxRateValidator` rejects zero (guards the division). (2) **Editing a record can no longer change its month/year** — `_isEditing` now renders a plain, non-editable "August 2026"-style header instead of the Month dropdown + Year field, with a one-line note that it can't be changed. This makes the whole duplicate-month-warning UI (dropdown disabling, inline warning, submit-disabling) structurally create-mode-only now — editing can never conflict with another record since it can no longer touch the field that uniqueness is keyed on. `updateRecord` stopped sending `year`/`month` in its request body at all (both become `null` from the editor, and the existing null-aware `'year': ?year` map syntax in the datasource already omits them, so the backend leaves the stored value untouched — no backend change needed). Live-verified end-to-end against the real database: created a real Aug 2026 record through the redesigned form and confirmed the exact expected computed USD landed server-side (1,000,000 Rs ÷ 280 → 3,571 USD; 600,000 Rs ÷ 280 → 2,143 USD, both matching to the dollar), opened it in edit mode and confirmed the static "August 2026" header with no dropdown/Year field, then deleted the test record via `psql` and confirmed the page reverted to the exact pre-test baseline (43 months, same totals). Full regression: 354 frontend tests (10 changed/added in the editor's own suite — the "editing flags a conflict with another record" test was rewritten to assert the opposite, since that scenario is now impossible by construction) + `flutter analyze` clean — no backend changes, so the backend suite was untouched.

The original roadmap completed at 6/6 (2026-08-22), then shrank to 2 active modules over the following day as Automations, Agency Reporting, and Finances were each removed by explicit instruction — see their retired bullets above for what they used to do — before growing to 3 with Leads (module 7) on 2026-08-23, then to 4 with Financial Reports (module 8) on 2026-08-25. Any further Payroll work (e.g. per-employee payslip PDFs, email delivery, recurring items, an advance/loan ledger) is a deliberate future enhancement, not a gap in what was scoped here. Company-wide revenue/profit/expense reporting was out of scope from 2026-08-23 until the user asked again on 2026-08-25 — see Financial Reports (module 8) above; it is explicitly *not* a reintroduction of the original Agency Reporting module (no P&L date-range endpoint, no compare-to-previous-period, no top-clients-by-profit — a plain monthly/yearly dashboard is all that was asked for this time).

## Company-wide Audit Log ("Logs")

Not part of the Admin Business Management roadmap above — this is the pre-existing, core `employee` feature's combined change-history feed (`AuditLogEntry`/`PaginatedAuditLog`/`companyAuditLogProvider`/`CompanyAuditLogSection`, backed by `GET /employees/audit-log`), which records every employee-field edit across the company (actor, employee, field, before/after, timestamp), searchable and paginated.

**Moved into its own "Logs" nav destination, and shared with HR/Manager, 2026-08-30** per explicit instruction ("Move company wide changes into logs page and it should be shown to only hr and admin") — it had previously rendered as a `CompanyAuditLogSection` `FormSection` bolted onto the bottom of the Admin Dashboard, gated by `audit.viewAll`, which only Super Admin held. Two changes, same shape as every other module's HR/Manager-sharing precedent in this file: (1) backend — `audit.viewAll` added to HR/Manager's permission array in `seed.ts` (Super Admin keeps it via the implicit "every known permission" grant; `EmployeesController`'s `@Permissions('audit.viewAll')` guard on `GET /employees/audit-log` is unchanged, since only who holds the permission changed, not the check); re-ran `npm run seed` against the dev DB to apply it — an HR/Manager user already signed in keeps the old (denied) state until they sign out and back in, same JWT-staleness caveat as every other permission-grant change here. (2) frontend — a new `LogsPage` (`frontend/lib/features/employee/presentation/pages/logs_page.dart`) is just a permission gate (`hasPermission('audit.viewAll')` → `AccessDeniedView`, matching every other Admin Business Management page's own top-of-`build()` convention) wrapping the *existing*, unmodified `CompanyAuditLogSection` in a `Padding`+`SingleChildScrollView` — no new widget logic, since the section itself (search box, pagination, row rendering) didn't need to change, only where it's mounted. `AdminDashboardPage` had the section, its `canViewAllAudit` permission check, and the `showCompanyAuditLog` prop it threaded through `_DashboardStats` all removed. `main.dart` gained a new `'Logs'` nav destination (`Icons.history_outlined`) in `_adminOnlyLabels` (hidden from Team Lead/Employee, same as every admin-tier module) **and** `_hrAndAdminOnlyLabels` (shared with HR/Manager, grouped under the Super Admin sidebar's "HR & Admin Features" heading alongside Clients & Projects/Payroll — deliberately *not* `_superAdminOnlyLabels`, unlike Financial Reports/Leads) — despite `_hrAndAdminOnlyLabels`'s doc comment historically describing "the other half of Admin Business Management," Logs isn't part of that section, just the same access tier, and the comment was reworded to say so. 5 new/changed frontend tests (2 in a new `logs_page_test.dart` — content shown for an `audit.viewAll` holder, `AccessDeniedView` otherwise — plus 3 nav-visibility tests in `widget_test.dart` mirroring the Clients & Projects/Payroll precedent). Full regression: 352 backend tests + `nest build` clean (seed.ts isn't unit-tested, and no other backend file changed), 375 frontend tests (5 new) + `flutter analyze` clean.

## Admin Dashboard Overview stats

The Admin Dashboard's plain Overview/Work Mode stat tiles (`AdminDashboardPage`/`_DashboardStats`, `frontend/lib/features/employee/presentation/pages/admin_dashboard_page.dart`) — Total Employees, Active, Work Mode breakdown, etc. — predate this file's per-feature tracking; two additions landed 2026-08-30 worth recording since neither is derivable from the surrounding code alone.

**Freelancers added as a 4th Work Mode tile**, per explicit instruction ("along with onsite, remote and hybrid, show freelancers as well"). A new `_WorkModeFreelancersCard` sits alongside On-site/Remote/Hybrid, showing the same active-freelancer count `_TotalFreelancersCard` already shows under Admin Business Management further down the page — two places, one figure, not two competing counts. Gated by the same `showPayrollRuns` (`payroll.manage`) flag as `_TotalFreelancersCard`, since that's the permission freelancer data lives behind; freelancers aren't `Employee` rows and have no `workMode` of their own, so this is a headcount-parity addition, not a real fourth work-mode bucket.

**Week-over-week deltas added to the Active and Pending Performance Reviews tiles**, per explicit instruction ("if new employees become active then active box will show +1", "if pending performance reviews filled and amount became 15 to 14 then show -1"). Before implementing, the comparison baseline was confirmed via AskUserQuestion — not "since yesterday" or "since last viewed," but **a fixed 7-day window** ("in one week... (+1 in last 7 days)"), shown as a `MetricCard.secondaryValue` line reading "+1 in last 7 days" / "-1 in last 7 days" / "No change in last 7 days" (formatted by a shared `_formatWeeklyDelta` helper; null while loading/erroring, so the tile just omits the line rather than flickering or showing stale text).

Both deltas are reconstructed entirely from data that already exists — **no new snapshot table, no cron job** — since the two domains already carry enough history:

- **Active employees** (`EmployeesService.getActiveEmployeeDelta`, `GET /employees/stats/active-delta?days=7`, `employees.read`): combines the current employee list with every "Employment Status" `EmployeeAuditLog` change recorded since the cutoff (`AuditLogRepository.findFieldChangesSince`, new). An employee's status "as of the cutoff" is the `oldValue` of the *earliest* such change after the cutoff if one exists, else their current status unchanged. An employee whose own `createdAt` is after the cutoff didn't exist yet, so they count as "not active back then" regardless of current status — this is what makes a new hire who's active today correctly show up as part of the +1, rather than being excluded from both sides of the comparison as a wash.
- **Pending performance reviews** (`PerformanceReviewsService.getPendingReviewsDelta`, `GET /performance-reviews/pending-delta?days=7`, `performance.manage`): `pendingNow` (count with `status == PENDING`) minus `pendingAsOfCutoff` — a new `PerformanceReviewRepository.countPendingAsOf(cutoff)` querying `createdAt <= cutoff AND (completedAt IS NULL OR completedAt > cutoff)`. Exact, not an approximation, because `completedAt` is a one-way marker in this schema — `unfinalizeReview` reverts `FINALIZED` back to `COMPLETED`, never back to `PENDING`, so a review can never re-enter the pending state once `completedAt` is set.

Both endpoints follow the controller route-ordering convention used throughout `EmployeesController`/`PerformanceReviewsController` (declared before their respective `@Get(':id')`, with the same "must come before" comment). 9 new backend tests (6 for `getActiveEmployeeDelta` covering the new-hire/status-change/multi-change/no-change cases, 3 for `getPendingReviewsDelta`), 9 new frontend tests (1 Work Mode Freelancers test with an active/inactive mix, 3 Active-tile delta tests — positive/negative/zero — 1 Pending Performance Reviews delta test, plus the existing Admin Business Management tests extended to also assert the new Work Mode Freelancers tile appears/hides correctly alongside `Total Freelancers`). Full regression: 361 backend tests + `nest build` clean, 380 frontend tests + `flutter analyze` clean.

## Shared `MetricCard` stat tile

`MetricCard` (`frontend/lib/shared/widgets/metric_card.dart`) is the one stat-tile widget almost every page's summary row uses (Admin Dashboard, Leads, Financial Reports, Payroll) — so a change to it is felt everywhere at once. Redesigned twice in quick succession 2026-08-30:

**Heading moved right after the icon, size increased**, per explicit instruction ("In stats boxes, please put stat heading right after the icon and make it little bigger than now") — previously the default layout (`labelFirst: false`, used by nearly every caller) put a small `bodySmall`/`textSecondary` caption *below* the value; only 2 Financial Reports money tiles opted into the alternate `labelFirst: true` layout (label above value, `titleSmall`/bold/`textPrimary`). Since the ask was for *all* stat boxes to read heading-then-value, the two layouts were collapsed into one — the `labelFirst` parameter was removed entirely (dead code once nothing set it to `false` on purpose) and the `labelFirst: true` styling became the only styling, including its `fontWeight: FontWeight.w700` override. Live-verified across the real dashboard — Overview, Work Mode, Payroll, and Admin Business Management tiles all picked up the new order automatically, no per-page changes needed. Full regression: 380 frontend tests + `flutter analyze` clean (the widget test suite asserts tile values/labels via `find.text`, not vertical order, so a shared-widget change this size didn't need any test rewrites, only re-runs).

**Font weight decreased, same day, immediately after** ("decrease the font weight") — the `w700` override was removed outright rather than swapped for a smaller explicit weight, letting the label fall back to `titleSmall`'s own theme weight (`FontWeight.w600`, the same "heading weight" `titleMedium`/`titleLarge` already use in `app_theme.dart`) — a deliberate one-step-down landing on an already-established value rather than inventing a new one-off weight just for this widget.

**Clients & Projects migrated onto the shared widget, same message** ("do this feature for leads, clients") — Leads already used `MetricCard` directly for all 6 of its stat tiles, so it inherited both changes above automatically with zero code changes there. Clients & Projects did not: `clients_projects_page.dart` had its own private `_StatTile` (icon → value → label, structurally identical to `MetricCard`'s *pre*-redesign layout, just duplicated rather than shared) backing both its summary row (Total Clients/Active Clients/Monthly Retainers/One-Time Projects) and its Health tab's row (Healthy/Attention Required/At Risk) — 7 call sites total. Deleted `_StatTile` and pointed all 7 at `MetricCard` instead — same fields (`label`/`value`/`color`/`icon`), so it was a drop-in swap with no call-site restructuring. `clients_projects_page_test.dart`'s existing assertions were already scoped via `find.byType(Wrap).first` rather than the tile's own type, so nothing there needed updating either. Full regression: 380 frontend tests + `flutter analyze` clean.

## Employee Requests: item requests merged into the general request

Per explicit instruction ("in employees, there is request an item and new request are separate features.. they should 1 single feature") — the Requests page (`frontend/lib/features/requests/presentation/pages/requests_page.dart`) had two separate submission entry points: "New request" (`_SubmitRequestDialog`, any subject/description/optional category) and "Request an item" (`_RequestItemDialog`, item name + purpose). These weren't just two dialogs for the same thing — an item request was a distinct `RequestKind.ITEM` that **skipped the reporting-manager approval step entirely**, landing straight in the HR/Admin queue (created directly at `MANAGER_APPROVED`), while a general request goes through the full Submitted → Manager Approval → HR Approval → Completed pipeline.

Since collapsing the UI without addressing that workflow difference would still leave two behaviors under one button, this was confirmed via AskUserQuestion before touching code: **fully unify** (chosen) — every request, including what used to be an item request, now goes through the identical Manager-then-HR workflow; "item" becomes just a value someone types into the existing free-text Category field (its hint text was updated to `"e.g. Item, Equipment, Document, Access"` to make that discoverable) — versus the rejected alternative of merely merging the UI into one dialog while still branching to two backend behaviors underneath.

Removed entirely: the "Request an item" button and `_RequestItemDialog`/`_RequestItemDialogState` from `requests_page.dart`; `RequestsService.submitItemRequest`, `POST /requests/items`, and `CreateItemRequestDto` on the backend; `submitItemRequest` from the frontend `RequestRepository` interface/impl/datasource and the `FakeRequestRepository` test helper. **Not removed**: `RequestKind.ITEM` itself stays defined in the enum (with a comment explaining it) purely so any pre-existing rows created before this change still deserialize correctly — no migration was run to convert them to `GENERAL`, since their historical fact ("this one skipped manager approval when it was submitted") is still accurate and harmless to leave as-is; no code path can ever set that kind again. `RequestKind.PROFILE_CHANGE` (a genuinely different, still-needed feature — self-service profile edits routed to HR/Admin — unrelated to what the user asked about) was left completely untouched, including its own manager-approval-skipping behavior. 1 backend test removed (the dedicated `submitItemRequest` spec) and 1 frontend test removed (the item-request submission flow), plus a stale `find.text('Request an item')` assertion fixed in the Super-Admin-hides-buttons test. Full regression: 360 backend tests + `nest build` clean, 379 frontend tests + `flutter analyze` clean.

## Tasks: list-row info and inline status

Two related changes to the Tasks module (`backend/src/features/tasks/`, `frontend/lib/features/tasks/`) landed 2026-08-30, per explicit instruction ("the task is shown that is fine.. it should also show who assigned it", "the status can be changed without clicking the task").

**Each row on `TasksPage` now shows who it's assigned to and by whom** — `Task.assignedByName` already existed on the entity (used on the detail page) but was never surfaced on the list; `_TaskRow`'s subtitle line reads `Assigned to {assigneeName} · {department} · Due {date} · Assigned by {assignedByName}` (previously the assignee's bare name had no "Assigned to" label at all, inconsistent with the labeled "Assigned by" clause added alongside it — fixed same-day after the user flagged the asymmetry in a screenshot annotation), with `maxLines: 1`/ellipsis added since the line was already tight.

**Status can now be changed directly from the row**, not just from the task's own detail page. `_TaskRow` (now a `ConsumerStatefulWidget`) replaces the previously-static `TaskStatusBadge` with a new `_InlineStatusMenu` — the same badge, now wrapped in a `PopupMenuButton` listing every `TaskStatus` value (a check mark against the current one), calling `TaskRepository.updateStatus` and invalidating the same three list providers (`myTasksProvider`/`tasksAssignedByMeProvider`/`teamTasksProvider`) the detail page's own `_StatusControl` already invalidates. Nested inside the row's own `InkWell` (which still opens the detail page on tap elsewhere in the row) — tapping the popup menu button consumes its own tap without also triggering the ancestor's, the same well-established pattern as any other trailing button inside a tappable list row. No backend change was needed here: this reuses the existing self-service-plus-authorized-tier rule `TasksService.updateStatus` already enforced; the frontend was the only place actually missing the shortcut, so an unauthorized attempt still surfaces the backend's rejection as a snackbar rather than silently failing or being hidden.

**A same-day detour, corrected before it shipped further:** editing a task's core fields (title/description/priority/due date/assignee) was briefly narrowed to `tasks.manage`-only per a literal reading of "Edit task is only possible for HR and admin" — removing the long-standing ability for a task's own assigner or the assignee's department head (a Team Lead) to edit it. The very next instruction ("make sure admin and TLs and HR can edit the tasks") reversed this explicitly, so `TasksService.canEdit` (and its frontend mirror `_canEditTask` on `TaskDetailPage`) were restored to their original three-tier check — `tasks.manage` (Super Admin/HR-Manager) OR the task's assigner OR the assignee's department head — with `updateStatus`'s non-self branch continuing to share the same helper (the brief `canOverrideStatus` rename was undone along with everything else). Net effect versus the pre-2026-08-30 baseline: unchanged. Task creation (`createTask`) was never touched either time — it's always been on its own independent authority path (`canAssignTo`, keyed off the *new* assignee's department). The backend `updateTask` describe block added during the detour was kept, but repointed at the restored (allow, not reject) outcomes, plus a new "rejects a genuine stranger" case — net new, real coverage `updateTask`'s authorization never had before this whole round-trip. Full regression: 364 backend tests + `nest build` clean, 383 frontend tests + `flutter analyze` clean.

## Top-bar announcement banner

Added 2026-09-02 per explicit instruction ("In the top bar... I want something like automated notice there. Like Happy new year on new year. Happy Birthday person name. New announcement, please check") — a slim, dismissible, auto-rotating strip shown directly under the top bar/AppBar on every page, on all three breakpoints. Confirmed via `AskUserQuestion` before building: visible to **every employee** (not just HR/Admin), a notice triggers the banner if posted **today** (no per-user read-tracking — a simpler, deliberately-scoped choice over building a real read-receipt system), the holiday name/date comes from the **existing Public Holidays table** (so "Happy New Year" just needs someone to add a "New Year's Day" row each year — holidays aren't recurring, see `Holiday` entity), and it's **dismissible per day** (a small X hides it for the rest of that calendar day, reappearing tomorrow).

Backend: new standalone `backend/src/features/announcements/` module — `GET /announcements/today`, deliberately **no `@Permissions` guard** (unlike the birthday/anniversary endpoints at `employees/birthdays/upcoming` and `employees/anniversaries/upcoming`, which stay `employees.manage`-gated for the notification bell's existing HR-only "Celebrations" feed) so every authenticated employee can see it. `AnnouncementsService` aggregates four already-existing data sources in one call (so the banner, mounted once in the app shell, doesn't fire four separate requests on every page load): `EmployeesService.getUpcomingBirthdays(0, 0)`/`getUpcomingWorkAnniversaries(0, 0)` (reused as-is, called with a zero window so only `daysUntil === 0` — today exactly — survives the existing filter, no changes needed to that service), `HolidaysService.getAll(year)` filtered to today's date, and `NoticesService.findAll()` filtered to notices whose `createdAt` falls on today's local calendar day (`NoticesModule` gained `exports: [NoticesService]`, previously unexported since nothing outside the module needed it).

Frontend: `frontend/lib/features/announcements/` (full read-only layered slice — domain/data/application, no write operations, mirrors the `holidays` module's shape) plus the actual widget, `frontend/lib/shared/widgets/announcement_banner.dart`. Rotates every 6s through whichever items are active (`Timer.periodic`, restarted only when the item count changes — not on every rebuild the timer's own tick causes) with a small dot-pagination indicator; each category gets a distinct icon/color reusing the notification bell's existing conventions (`Icons.cake_outlined`/secondary for birthdays, `Icons.celebration_outlined`/accentTeal for anniversaries, `Icons.campaign_outlined`/primary for notices) plus a new one for holidays (`Icons.festival_outlined`/warning). The per-day dismiss flag is stored via a new `frontend/lib/core/storage/announcement_dismissal_storage.dart`, reusing the app's existing `FlutterSecureStorage` instance (`secureStorageProvider`, already used for auth tokens) rather than adding a new local-storage dependency — keyed by an ISO date string, compared on each app load.

Wired into `ResponsiveScaffold` (`frontend/lib/core/layout/responsive_scaffold.dart`) identically on all three breakpoints — inserted directly below the top chrome (desktop's `_TopBar` row, tablet/mobile's `AppBar`) and above the page content, so it always sits in the same visual position "where the page heading and notifications are" regardless of screen size. Live-verified end-to-end (temporary real-data mutations, reverted after): set a real employee's `dateOfBirth` to today → birthday item appeared; created a real `Holiday` dated today → holiday item appeared; created a real `Notice` → notice item appeared; confirmed all three rotate with the dot indicator advancing, confirmed dismiss hides the whole banner and persists across navigation and page reload, confirmed the banner disappears entirely once nothing is active (not an empty strip), and confirmed correct rendering on desktop, tablet, and mobile viewports.

## User Dashboard: Logs moved off-dashboard, "My Team" detail

Three related changes to the User Dashboard (`frontend/lib/features/employee/presentation/pages/user_dashboard_page.dart` — shared by every non-admin role: Employee and Team Lead alike) landed 2026-09-02 per explicit instruction ("In TL user dashboard... change history should go on Logs page... Team members heading should be My Team... show their basic details like company email, time in the company, last performance review, birthday etc").

**Change History relocated to Logs.** The dashboard's own `EmployeeAuditLogPanel` (a viewer's own field-change history, `employeeId: null`) used to be the last section on every non-admin viewer's dashboard — removed from there entirely. `LogsPage` (`frontend/lib/features/employee/presentation/pages/logs_page.dart`, previously `audit.viewAll`-gated with `AccessDeniedView` for everyone else) now branches instead: `audit.viewAll` holders (Super Admin, HR/Manager) still get the unchanged company-wide `CompanyAuditLogSection`; everyone else gets that same `EmployeeAuditLogPanel` — their own change history, just relocated. `main.dart`'s `'Logs'` nav destination moved out of `_adminOnlyLabels` (it stays in `_hrAndAdminOnlyLabels` purely so the Super Admin's own sidebar still groups it under "HR & Admin Features") so every role now sees a Logs entry, not just Super Admin/HR-Manager.

**"Team Members" renamed "My Team".** Cosmetic only — `_TeamMembersSection`'s `FormSection` title.

**Team member rows gained company email, tenure, birthday, and last-review status** — previously just name + designation. Reused existing pieces rather than building new ones: `InfoChip` (`employee_status_badges.dart`) and `formatTenure`/`formatMonthDay` (`shared/utils/date_format.dart`), the exact same building blocks the Employee Directory's own `_EmployeeCard` already uses, so the visual language matches. Two backend gaps needed closing, both scoped narrowly to "a manager's own direct reports," not opened up company-wide:
- **Birthdays were nulled out for this exact call.** `EmployeesService.applyFieldVisibility` redacts `dateOfBirth` for any viewer who isn't the employee themself or an `employees.manage` holder — which is every Team Lead, since they only hold `employees.read`. Rather than loosening that redaction generally, `getMyDirectReports` now passes a new `{ revealBirthday: true }` option through to just that one call site — every report in that list is by construction the caller's own direct report (the query is already scoped to `reportingManagerId`), so revealing birthdays there is a materially narrower exposure than the company-wide directory guard exists to prevent.
- **Latest-review data was `performance.manage`-gated entirely** (`GET /performance-reviews/latest-by-employee`, company-wide, used by the Employee Directory). New `GET /performance-reviews/my-team/latest` (`PerformanceReviewsService.getLatestForMyTeam`) deliberately carries **no `@Permissions` guard** — mirrors the pre-existing `getPendingManagerAction`'s identity-scoped pattern (finds the caller's own employee record, filters to their direct reports' ids server-side) rather than reusing the permission-gated endpoint. Frontend: `latestPerformanceReviewsForMyTeamProvider`, parallel to the Directory's own `latestPerformanceReviewsByEmployeeProvider`.

Live-verified against real data as a real Team Lead (not just Super Admin) — a `pending_invite` Team Lead account (9 real direct reports) was temporarily activated with a throwaway password to log in as, confirmed every report row shows company email/tenure/birthday/review status correctly (including a report's real birthday, e.g. "Feb 28"), confirmed Logs shows that Team Lead's own real change history (not company-wide, not access-denied), then the test account's `status`/`passwordHash` were restored to their exact original values afterward. Full regression: `nest build` clean, 33/33 `widget_test.dart` + `logs_page_test.dart` passing (two pre-existing tests updated for the new behavior: the old "hides Logs from the nav for a plain employee" assertion inverted, and `'Team Members'` → `'My Team'`).

## Requests: manager notification, "No need" for skipped manager approval, requester identity, Request History

Four related changes to `backend/src/features/requests/` and `frontend/lib/features/requests/`, 2026-09-02, per explicit instruction and a live bug report caught while testing the previous change.

**A GENERAL request now notifies the reporting manager immediately.** Previously the Requests module created zero notifications of any kind — a manager only ever learned about a pending request by opening the Requests page themselves. `RequestsService.submit` now looks up the requester's `reportingManagerId`, resolves that manager's `userId`, and calls the existing `NotificationsService.create` (unchanged, already used by tasks/leave/payroll/performance-reviews) with a new `NotificationLinkTarget.REQUESTS` — migration `1789700000000-AddRequestsNotificationLinkTarget` adds the enum value, following the exact precedent `AddPerformanceReviewNotificationLinkTarget` set. `PROFILE_CHANGE` requests skip this — they skip the manager step entirely (see below), so there's no manager to notify. `RequestsModule` gained `NotificationsModule` as an import to make this possible.

**"No need" now shows wherever manager-approval status is displayed, for a PROFILE_CHANGE request.** `RequestResponse`/`EmployeeRequest` (backend and frontend) gained a `kind` field — it existed on the backend entity all along but was never exposed in the API response, so the frontend had no way to distinguish a `profile_change` request (created directly at `MANAGER_APPROVED`, no manager ever acts on it) from a `general` one that a manager genuinely approved. A new `_managerApprovalLabel` helper (`requests_page.dart`) returns "No need"/"Pending"/"Rejected"/"Approved" appropriately, shown on `_MyRequestRow` (the requester's own view) and `_PendingHrRequestRow` (HR's queue, replacing the old `'Approved by manager: ${managerDecisionByName ?? '—'}'` line, which used to read "Approved by manager: —" for a profile-change request — technically true in that no rejection happened, but read as if something was missing rather than that the step never applied).

**A profile-change request's "field: old → new" description line no longer shows a placeholder for a never-set old value.** `RequestsService.submitProfileChangeRequest`'s description builder used to always prefix `"{field}: {old ?? '—'} → {new}"`; it now omits the `"{old}: "` prefix entirely when `oldValue` is null, so "Emergency Contact Name → Sana Hussain" replaces the old "Emergency Contact Name: — → Sana Hussain". Backend-only, no frontend re-parsing involved (the description is pre-built server-side and rendered verbatim) — existing rows keep their original text as-is, since this only affects newly-created descriptions.

**Every request row someone other than the requester reviews now shows who it's from and when, via a new shared `_RequesterHeader` widget** (avatar + name + `formatDisplayDateTime(createdAt)`) — added to `_PendingManagerRequestRow` and `_PendingHrRequestRow`, which previously only showed a plain `'${subject} — ${requesterName}'` text line and no timestamp at all. The backend response already carried `requesterPhotoUrl`/`createdAt` end-to-end; nothing new was needed there.

**"Request History" — added same day**, after live-testing surfaced a real gap: rejecting Aamna Irfan's profile-change request made it vanish from every list with no record of who decided it or when, since `_PendingHrApprovalSection`/`_PendingMyApprovalSection` only ever query `SUBMITTED`/`MANAGER_APPROVED` requests — a decided (`REJECTED`/`COMPLETED`) request had nowhere to go. New `RequestsService.getHistory()` (`users.manage`-gated, company-wide, `GET /requests/history`) and `getHistoryForMyTeam(actorUserId)` (no `@Permissions` guard, scoped to the caller's own direct reports — mirrors `getPendingManagerAction`/`getLatestForMyTeam`'s identity-scoped pattern, `GET /requests/history/mine`) both merge `REJECTED`+`COMPLETED` requests, sorted newest-decision-first via a `decidedAt` helper (`hrDecisionAt ?? managerDecisionAt`), capped to the 30 most recent — the same "bounded history, not a time-window cutoff" approach the notification bell's `_maxDecidedHistory` already uses, so nothing vanishes just because time passed. `RequestsPage` shows a new `_RequestHistorySection` (company-wide for `users.manage` holders, team-scoped otherwise) to anyone who can already see a pending-approval section — reuses `_RequesterHeader`/`_RequestStatusBadge` and adds a "Decided by {name} · {date}" line plus the rejection reason when present.

Live-verified end-to-end with real accounts (two `pending_invite` employees — a Team Lead and one of their direct reports — temporarily activated, then restored to their exact original `status`/`passwordHash` afterward): submitted a GENERAL request as the report → confirmed the manager received the persisted notification (`"Babar Hussain submitted a request — \"Need a new monitor\" — awaiting your approval"`, `linkTarget: 'requests'`) in their bell's Reminders section; submitted a PROFILE_CHANGE request → confirmed the description read `"Emergency Contact Name → Sana Hussain"` (no placeholder) and `"Manager Approval: No need"` showed correctly in HR's queue; confirmed the requester's avatar/name/timestamp render on both pending-approval row types. The Request History section was confirmed directly against the user's own real action taken mid-session — rejecting Aamna Irfan's real profile-change request — which appeared at the top of History reading "Decided by Noushad Ranani · Sep 02, 2026, 6:06 PM" once the fix shipped. Test data (2 synthetic requests + 1 notification) was deleted directly afterward — there's no delete-request feature by design, since requests are a workflow record — and both test accounts were restored to their exact original database state (verified byte-for-byte against the original `passwordHash`). Fixed a real regression this surfaced in `employees.service.spec.ts`: an existing `getMyDirectReports` test asserted `dateOfBirth` stays masked for a manager without `employees.manage` — true before this session, deliberately reversed by the "My Team" birthday feature (see the "User Dashboard" section above) — updated to assert the field is now revealed, keeping every other masked field's assertion unchanged. Full regression: 368 backend tests (5 new: 2 notification, 2 history, kept the reversed DOB test's 1) + `nest build` clean, 388 frontend tests (5 new) + `flutter analyze` clean.

**Request History made more compact, 2026-09-03** — `_RequestHistoryRow` was originally built from the same pieces as the pending-approval rows (`_RequesterHeader` avatar + full-size text), too tall for a feed meant to hold many decided requests. Dropped the avatar, combined subject/requester onto one line, switched to `bodySmall`/`labelSmall` text throughout, and added a `dense` param to `_RequestStatusBadge` for a smaller badge. Also fixed "Decided by" rendering a raw email address for a handful of older decisions recorded before `resolveActorName` existed on the backend — a frontend-only `_displayActorName` helper derives a Title-Case name from the email's local part (`noushad.ranani@...` → "Noushad Ranani") purely for display, without rewriting the historical DB snapshot (the entity's decision-name fields are intentionally immutable point-in-time snapshots). Live-verified against real historical rows in the browser (both a genuine full-name record and an old email-only one), frontend suite green (388/388) + `flutter analyze` clean.

**Request History row restructured into a 3-line layout with the decision moved to the right, 2026-09-03**, per explicit instruction. `_RequestHistoryRow`'s `build()` changed from a single `Column` to a `Row` with two sides: the left `Expanded` column now shows exactly three lines — requester name alone (no subject), the full change/description, then only the decided-at date/time — and the right column stacks the status badge (Completed/Rejected) above a renamed "Approved by {name}" line (previously "Decided by {name} · {date}" all on one line on the left). The rejection reason, when present, still appears as a line under the date on the left. Updated the 3 test assertions in `requests_page_test.dart` that checked for the old "Decided by" wording to expect "Approved by" instead. Live-verified against the same real historical rows (Aamna Irfan's rejected request, Abdul Samad Charan's completed ones) in the browser — confirmed the 3-line left layout and the right-aligned badge+approver stack render correctly with no overflow. Full regression: 390/390 frontend tests + `flutter analyze` clean (backend untouched, no backend changes this round).

## Development Workflow
1. Understand the requirement.
2. Design the architecture.
3. Create/update the implementation plan.
4. Implement one feature/module at a time.
5. Keep the app compiling after every change.
6. Refactor when necessary.
7. Add tests where appropriate.
8. Update docs after each completed feature.

Never generate the whole app at once. Never break existing functionality when adding new features.

## Definition of Done
Compiles; follows project architecture; respects RBAC; validation implemented; loading states; empty states; error handling; responsive layouts; docs updated; no unnecessary duplication. Repository stays clean and production-ready after every change.
