/// Raw values matching the backend's ExpenseCategory enum.
class ExpenseCategory {
  static const rentUtilities = 'rent_utilities';
  static const softwareTools = 'software_tools';
  static const marketing = 'marketing';
  static const vendorPayment = 'vendor_payment';
  static const taxes = 'taxes';
  static const commissions = 'commissions';
  static const other = 'other';

  static const values = [
    rentUtilities,
    softwareTools,
    marketing,
    vendorPayment,
    taxes,
    commissions,
    other,
  ];
}
